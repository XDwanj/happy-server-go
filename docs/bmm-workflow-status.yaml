# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-09T06:01:14.882Z"
project: "happy-server-go"
project_type: "go-server"
selected_track: "method"
field_type: "greenfield"
workflow_path: "paths/method-greenfield.yaml"
workflow_status:
  # Phase 0: Discovery (Optional)
  - phase: 0
    name: "Discovery"
    workflows:
      - id: "research"
        name: "研究"
        status: "optional"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"
        note: "技术/竞争分析"

      - id: "product-brief"
        name: "产品简报"
        status: "optional"
        agent: "analyst"
        command: "/bmad:bmm:workflows:create-product-brief"
        note: "战略产品规划 (推荐用于 greenfield 项目)"

  # Phase 1: Planning
  - phase: 1
    name: "Planning"
    workflows:
      - id: "prd"
        name: "产品需求文档"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"
        note: "包含功能需求和非功能需求的 PRD"

      - id: "create-ux-design"
        name: "UX 设计"
        status: "conditional"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"
        note: "如果有 UI - 在 PRD 之后由用户/代理决定是否需要"

  # Phase 2: Solutioning
  - phase: 2
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        name: "系统架构"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
        note: "greenfield 项目的完整系统设计"

      - id: "create-epics-and-stories"
        name: "Epic 和 Story"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-stories"
        note: "将 PRD 分解为可实施的 epic 和 story，包含完整上下文 (PRD + UX + Architecture)"

      - id: "test-design"
        name: "测试设计"
        status: "recommended"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"
        note: "门检之前的系统级可测试性评估 - 自动检测系统级模式"

      - id: "validate-architecture"
        name: "架构验证"
        status: "optional"
        agent: "architect"
        command: "/bmad:bmm:workflows:validate-architecture"
        note: "架构完整性质量检查"

      - id: "implementation-readiness"
        name: "实施就绪检查"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"
        note: "验证 PRD + Architecture + Epics + UX (可选)"

  # Phase 3: Implementation
  - phase: 3
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        name: "Sprint 规划"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "创建 sprint 计划 - 后续工作在此跟踪"
